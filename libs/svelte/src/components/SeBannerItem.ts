/* eslint-disable */
/* tslint:disable */
// @ts-nocheck
import { Components, JSX } from '@se/web-ui';


interface SeBannerItemProps {
  
  /** Sets the background image for your banner item. */
  imageUrl?: Components.SeBannerItem["imageUrl"]
  
  /** Indicates the active banner item in your banner component. */
  active?: Components.SeBannerItem["active"]
}

interface SeBannerItemEvents {
  
  /** Send an event when the banner item changes */
  didChange: Parameters<JSX.SeBannerItem["onDidChange"]>[0]
}

interface SeBannerItemSlots {
  default: any
}
  
/* generated by Svelte v3.44.1 */
import {
	SvelteComponent,
	binding_callbacks,
	create_slot,
	detach,
	element,
	flush,
	get_all_dirty_from_scope,
	get_slot_changes,
	init,
	insert,
	listen,
	safe_not_equal,
	set_custom_element_data,
	transition_in,
	transition_out,
	update_slot_base
} from "svelte/internal";

import { createEventDispatcher, onMount } from 'svelte';

function create_fragment(ctx) {
	let se_banner_item;
	let current;
	let mounted;
	let dispose;
	const default_slot_template = /*#slots*/ ctx[6].default;
	const default_slot = create_slot(default_slot_template, ctx, /*$$scope*/ ctx[5], null);

	return {
		c() {
			se_banner_item = element("se-banner-item");
			if (default_slot) default_slot.c();
			set_custom_element_data(se_banner_item, "image-url", /*imageUrl*/ ctx[0]);
			set_custom_element_data(se_banner_item, "active", /*active*/ ctx[1]);
		},
		m(target, anchor) {
			insert(target, se_banner_item, anchor);

			if (default_slot) {
				default_slot.m(se_banner_item, null);
			}

			/*se_banner_item_binding*/ ctx[7](se_banner_item);
			current = true;

			if (!mounted) {
				dispose = listen(se_banner_item, "didChange", /*onEvent*/ ctx[3]);
				mounted = true;
			}
		},
		p(ctx, [dirty]) {
			if (default_slot) {
				if (default_slot.p && (!current || dirty & /*$$scope*/ 32)) {
					update_slot_base(
						default_slot,
						default_slot_template,
						ctx,
						/*$$scope*/ ctx[5],
						!current
						? get_all_dirty_from_scope(/*$$scope*/ ctx[5])
						: get_slot_changes(default_slot_template, /*$$scope*/ ctx[5], dirty, null),
						null
					);
				}
			}

			if (!current || dirty & /*imageUrl*/ 1) {
				set_custom_element_data(se_banner_item, "image-url", /*imageUrl*/ ctx[0]);
			}

			if (!current || dirty & /*active*/ 2) {
				set_custom_element_data(se_banner_item, "active", /*active*/ ctx[1]);
			}
		},
		i(local) {
			if (current) return;
			transition_in(default_slot, local);
			current = true;
		},
		o(local) {
			transition_out(default_slot, local);
			current = false;
		},
		d(detaching) {
			if (detaching) detach(se_banner_item);
			if (default_slot) default_slot.d(detaching);
			/*se_banner_item_binding*/ ctx[7](null);
			mounted = false;
			dispose();
		}
	};
}

function instance($$self, $$props, $$invalidate) {
	let { $$slots: slots = {}, $$scope } = $$props;
	let __ref;
	let __mounted = false;
	const dispatch = createEventDispatcher();
	let { imageUrl = undefined } = $$props;
	let { active = undefined } = $$props;
	const getWebComponent = () => __ref;

	onMount(() => {
		__mounted = true;
	});

	const setProp = (prop, value) => {
		if (__ref) $$invalidate(2, __ref[prop] = value, __ref);
	};

	const onEvent = e => {
		e.stopPropagation();
		dispatch(e.type, e.detail);
	};

	function se_banner_item_binding($$value) {
		binding_callbacks[$$value ? 'unshift' : 'push'](() => {
			__ref = $$value;
			$$invalidate(2, __ref);
		});
	}

	$$self.$$set = $$props => {
		if ('imageUrl' in $$props) $$invalidate(0, imageUrl = $$props.imageUrl);
		if ('active' in $$props) $$invalidate(1, active = $$props.active);
		if ('$$scope' in $$props) $$invalidate(5, $$scope = $$props.$$scope);
	};

	return [
		imageUrl,
		active,
		__ref,
		onEvent,
		getWebComponent,
		$$scope,
		slots,
		se_banner_item_binding
	];
}

class SeBannerItem extends SvelteComponent {
  $$prop_def: SeBannerItemProps;
  $$events_def: SeBannerItemEvents;
  $$slot_def: SeBannerItemSlots;

  $on<K extends keyof SeBannerItemEvents>(type: K, callback: (e: SeBannerItemEvents[K]) => any): () => void {
	  return super.$on(type, callback);
	}

  $set($$props: Partial<SeBannerItemProps>): void {
	  super.$set($$props);
	}

	constructor(options) {
		super();

		init(this, options, instance, create_fragment, safe_not_equal, {
			imageUrl: 0,
			active: 1,
			getWebComponent: 4
		});
	}

	get imageUrl() {
		return this.$$.ctx[0];
	}

	set imageUrl(imageUrl) {
		this.$$set({ imageUrl });
		flush();
	}

	get active() {
		return this.$$.ctx[1];
	}

	set active(active) {
		this.$$set({ active });
		flush();
	}

	get getWebComponent(): HTMLSeBannerItemElement | undefined {
		return this.$$.ctx[4];
	}
}

export default SeBannerItem;