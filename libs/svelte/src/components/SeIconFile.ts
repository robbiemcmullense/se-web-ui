/* eslint-disable */
/* tslint:disable */
// @ts-nocheck
import { Components, JSX } from '@se/web-ui';


interface SeIconFileProps {
  
  /** Text displayed inside the SVG icon. The text cannot be longer than 4 character. */
  value?: Components.SeIconFile["value"]
  
  /** Optional property to define if the icon should act as a button (clickable). */
  option?: Components.SeIconFile["option"]
  
  /** Defines the size of an icon.
`nano` sets the size to 14px.
`small` sets the size to 24px.(default setting)
`medium` sets the size to 32px.
`large` sets the size to 52px.
`xlarge` sets the size to 62px. */
  size?: Components.SeIconFile["size"]
  
  /** Optional property that defines the background color of the button.
The default color will be inherited from its parent. */
  color?: Components.SeIconFile["color"]
  
  /** Optional property that defines if the button is disabled.  Set to `false` by default. */
  disabled?: Components.SeIconFile["disabled"]
}

interface SeIconFileEvents {
  
}

interface SeIconFileSlots {
  default: any
}
  
/* generated by Svelte v3.42.6 */
import {
	SvelteComponent,
	binding_callbacks,
	create_slot,
	detach,
	element,
	flush,
	get_all_dirty_from_scope,
	get_slot_changes,
	init,
	insert,
	safe_not_equal,
	set_custom_element_data,
	transition_in,
	transition_out,
	update_slot_base
} from "svelte/internal";

import { createEventDispatcher, onMount } from 'svelte';

function create_fragment(ctx) {
	let se_icon_file;
	let current;
	const default_slot_template = /*#slots*/ ctx[8].default;
	const default_slot = create_slot(default_slot_template, ctx, /*$$scope*/ ctx[7], null);

	return {
		c() {
			se_icon_file = element("se-icon-file");
			if (default_slot) default_slot.c();
			set_custom_element_data(se_icon_file, "value", /*value*/ ctx[0]);
			set_custom_element_data(se_icon_file, "option", /*option*/ ctx[1]);
			set_custom_element_data(se_icon_file, "size", /*size*/ ctx[2]);
			set_custom_element_data(se_icon_file, "color", /*color*/ ctx[3]);
			set_custom_element_data(se_icon_file, "disabled", /*disabled*/ ctx[4]);
		},
		m(target, anchor) {
			insert(target, se_icon_file, anchor);

			if (default_slot) {
				default_slot.m(se_icon_file, null);
			}

			/*se_icon_file_binding*/ ctx[9](se_icon_file);
			current = true;
		},
		p(ctx, [dirty]) {
			if (default_slot) {
				if (default_slot.p && (!current || dirty & /*$$scope*/ 128)) {
					update_slot_base(
						default_slot,
						default_slot_template,
						ctx,
						/*$$scope*/ ctx[7],
						!current
						? get_all_dirty_from_scope(/*$$scope*/ ctx[7])
						: get_slot_changes(default_slot_template, /*$$scope*/ ctx[7], dirty, null),
						null
					);
				}
			}

			if (!current || dirty & /*value*/ 1) {
				set_custom_element_data(se_icon_file, "value", /*value*/ ctx[0]);
			}

			if (!current || dirty & /*option*/ 2) {
				set_custom_element_data(se_icon_file, "option", /*option*/ ctx[1]);
			}

			if (!current || dirty & /*size*/ 4) {
				set_custom_element_data(se_icon_file, "size", /*size*/ ctx[2]);
			}

			if (!current || dirty & /*color*/ 8) {
				set_custom_element_data(se_icon_file, "color", /*color*/ ctx[3]);
			}

			if (!current || dirty & /*disabled*/ 16) {
				set_custom_element_data(se_icon_file, "disabled", /*disabled*/ ctx[4]);
			}
		},
		i(local) {
			if (current) return;
			transition_in(default_slot, local);
			current = true;
		},
		o(local) {
			transition_out(default_slot, local);
			current = false;
		},
		d(detaching) {
			if (detaching) detach(se_icon_file);
			if (default_slot) default_slot.d(detaching);
			/*se_icon_file_binding*/ ctx[9](null);
		}
	};
}

function instance($$self, $$props, $$invalidate) {
	let { $$slots: slots = {}, $$scope } = $$props;
	let __ref;
	let __mounted = false;
	const dispatch = createEventDispatcher();
	let { value = undefined } = $$props;
	let { option = undefined } = $$props;
	let { size = undefined } = $$props;
	let { color = undefined } = $$props;
	let { disabled = undefined } = $$props;
	const getWebComponent = () => __ref;

	onMount(() => {
		__mounted = true;
	});

	const setProp = (prop, value) => {
		if (__ref) $$invalidate(5, __ref[prop] = value, __ref);
	};

	const onEvent = e => {
		e.stopPropagation();
		dispatch(e.type, e.detail);
	};

	function se_icon_file_binding($$value) {
		binding_callbacks[$$value ? 'unshift' : 'push'](() => {
			__ref = $$value;
			$$invalidate(5, __ref);
		});
	}

	$$self.$$set = $$props => {
		if ('value' in $$props) $$invalidate(0, value = $$props.value);
		if ('option' in $$props) $$invalidate(1, option = $$props.option);
		if ('size' in $$props) $$invalidate(2, size = $$props.size);
		if ('color' in $$props) $$invalidate(3, color = $$props.color);
		if ('disabled' in $$props) $$invalidate(4, disabled = $$props.disabled);
		if ('$$scope' in $$props) $$invalidate(7, $$scope = $$props.$$scope);
	};

	return [
		value,
		option,
		size,
		color,
		disabled,
		__ref,
		getWebComponent,
		$$scope,
		slots,
		se_icon_file_binding
	];
}

class SeIconFile extends SvelteComponent {
  $$prop_def: SeIconFileProps;
  $$events_def: SeIconFileEvents;
  $$slot_def: SeIconFileSlots;

  $on<K extends keyof SeIconFileEvents>(type: K, callback: (e: SeIconFileEvents[K]) => any): () => void {
	  return super.$on(type, callback);
	}

  $set($$props: Partial<SeIconFileProps>): void {
	  super.$set($$props);
	}

	constructor(options) {
		super();

		init(this, options, instance, create_fragment, safe_not_equal, {
			value: 0,
			option: 1,
			size: 2,
			color: 3,
			disabled: 4,
			getWebComponent: 6
		});
	}

	get value() {
		return this.$$.ctx[0];
	}

	set value(value) {
		this.$$set({ value });
		flush();
	}

	get option() {
		return this.$$.ctx[1];
	}

	set option(option) {
		this.$$set({ option });
		flush();
	}

	get size() {
		return this.$$.ctx[2];
	}

	set size(size) {
		this.$$set({ size });
		flush();
	}

	get color() {
		return this.$$.ctx[3];
	}

	set color(color) {
		this.$$set({ color });
		flush();
	}

	get disabled() {
		return this.$$.ctx[4];
	}

	set disabled(disabled) {
		this.$$set({ disabled });
		flush();
	}

	get getWebComponent(): HTMLSeIconFileElement | undefined {
		return this.$$.ctx[6];
	}
}

export default SeIconFile;