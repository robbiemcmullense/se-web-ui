/* eslint-disable */
/* tslint:disable */
// @ts-nocheck
import { Components, JSX } from '@se/web-ui';


interface SeAuthenticationProps {
  
  /** The title of your authentication screen. */
  appTitle?: Components.SeAuthentication["appTitle"]
  
  /** The version number you want to display. */
  version?: Components.SeAuthentication["version"]
  
  /** The domain you want to display.  If set to `ecostruxure`, it renders an EcoStruxure icon. */
  domain?: Components.SeAuthentication["domain"]
  
  /** The copyright you would like to display. */
  copyright?: Components.SeAuthentication["copyright"]
  
  /** Sets the background image for your authentication screen. */
  imageUrl?: Components.SeAuthentication["imageUrl"]
  
  /** If set to `true`, this will hide the authentication screen. */
  hide?: Components.SeAuthentication["hide"]
}

interface SeAuthenticationEvents {
  
}

interface SeAuthenticationSlots {
  default: any
}
  
/* generated by Svelte v3.44.1 */
import {
	SvelteComponent,
	binding_callbacks,
	create_slot,
	detach,
	element,
	flush,
	get_all_dirty_from_scope,
	get_slot_changes,
	init,
	insert,
	safe_not_equal,
	set_custom_element_data,
	transition_in,
	transition_out,
	update_slot_base
} from "svelte/internal";

import { createEventDispatcher, onMount } from 'svelte';

function create_fragment(ctx) {
	let se_authentication;
	let current;
	const default_slot_template = /*#slots*/ ctx[9].default;
	const default_slot = create_slot(default_slot_template, ctx, /*$$scope*/ ctx[8], null);

	return {
		c() {
			se_authentication = element("se-authentication");
			if (default_slot) default_slot.c();
			set_custom_element_data(se_authentication, "app-title", /*appTitle*/ ctx[0]);
			set_custom_element_data(se_authentication, "version", /*version*/ ctx[1]);
			set_custom_element_data(se_authentication, "domain", /*domain*/ ctx[2]);
			set_custom_element_data(se_authentication, "copyright", /*copyright*/ ctx[3]);
			set_custom_element_data(se_authentication, "image-url", /*imageUrl*/ ctx[4]);
			set_custom_element_data(se_authentication, "hide", /*hide*/ ctx[5]);
		},
		m(target, anchor) {
			insert(target, se_authentication, anchor);

			if (default_slot) {
				default_slot.m(se_authentication, null);
			}

			/*se_authentication_binding*/ ctx[10](se_authentication);
			current = true;
		},
		p(ctx, [dirty]) {
			if (default_slot) {
				if (default_slot.p && (!current || dirty & /*$$scope*/ 256)) {
					update_slot_base(
						default_slot,
						default_slot_template,
						ctx,
						/*$$scope*/ ctx[8],
						!current
						? get_all_dirty_from_scope(/*$$scope*/ ctx[8])
						: get_slot_changes(default_slot_template, /*$$scope*/ ctx[8], dirty, null),
						null
					);
				}
			}

			if (!current || dirty & /*appTitle*/ 1) {
				set_custom_element_data(se_authentication, "app-title", /*appTitle*/ ctx[0]);
			}

			if (!current || dirty & /*version*/ 2) {
				set_custom_element_data(se_authentication, "version", /*version*/ ctx[1]);
			}

			if (!current || dirty & /*domain*/ 4) {
				set_custom_element_data(se_authentication, "domain", /*domain*/ ctx[2]);
			}

			if (!current || dirty & /*copyright*/ 8) {
				set_custom_element_data(se_authentication, "copyright", /*copyright*/ ctx[3]);
			}

			if (!current || dirty & /*imageUrl*/ 16) {
				set_custom_element_data(se_authentication, "image-url", /*imageUrl*/ ctx[4]);
			}

			if (!current || dirty & /*hide*/ 32) {
				set_custom_element_data(se_authentication, "hide", /*hide*/ ctx[5]);
			}
		},
		i(local) {
			if (current) return;
			transition_in(default_slot, local);
			current = true;
		},
		o(local) {
			transition_out(default_slot, local);
			current = false;
		},
		d(detaching) {
			if (detaching) detach(se_authentication);
			if (default_slot) default_slot.d(detaching);
			/*se_authentication_binding*/ ctx[10](null);
		}
	};
}

function instance($$self, $$props, $$invalidate) {
	let { $$slots: slots = {}, $$scope } = $$props;
	let __ref;
	let __mounted = false;
	const dispatch = createEventDispatcher();
	let { appTitle = undefined } = $$props;
	let { version = undefined } = $$props;
	let { domain = undefined } = $$props;
	let { copyright = undefined } = $$props;
	let { imageUrl = undefined } = $$props;
	let { hide = undefined } = $$props;
	const getWebComponent = () => __ref;

	onMount(() => {
		__mounted = true;
	});

	const setProp = (prop, value) => {
		if (__ref) $$invalidate(6, __ref[prop] = value, __ref);
	};

	const onEvent = e => {
		e.stopPropagation();
		dispatch(e.type, e.detail);
	};

	function se_authentication_binding($$value) {
		binding_callbacks[$$value ? 'unshift' : 'push'](() => {
			__ref = $$value;
			$$invalidate(6, __ref);
		});
	}

	$$self.$$set = $$props => {
		if ('appTitle' in $$props) $$invalidate(0, appTitle = $$props.appTitle);
		if ('version' in $$props) $$invalidate(1, version = $$props.version);
		if ('domain' in $$props) $$invalidate(2, domain = $$props.domain);
		if ('copyright' in $$props) $$invalidate(3, copyright = $$props.copyright);
		if ('imageUrl' in $$props) $$invalidate(4, imageUrl = $$props.imageUrl);
		if ('hide' in $$props) $$invalidate(5, hide = $$props.hide);
		if ('$$scope' in $$props) $$invalidate(8, $$scope = $$props.$$scope);
	};

	return [
		appTitle,
		version,
		domain,
		copyright,
		imageUrl,
		hide,
		__ref,
		getWebComponent,
		$$scope,
		slots,
		se_authentication_binding
	];
}

class SeAuthentication extends SvelteComponent {
  $$prop_def: SeAuthenticationProps;
  $$events_def: SeAuthenticationEvents;
  $$slot_def: SeAuthenticationSlots;

  $on<K extends keyof SeAuthenticationEvents>(type: K, callback: (e: SeAuthenticationEvents[K]) => any): () => void {
	  return super.$on(type, callback);
	}

  $set($$props: Partial<SeAuthenticationProps>): void {
	  super.$set($$props);
	}

	constructor(options) {
		super();

		init(this, options, instance, create_fragment, safe_not_equal, {
			appTitle: 0,
			version: 1,
			domain: 2,
			copyright: 3,
			imageUrl: 4,
			hide: 5,
			getWebComponent: 7
		});
	}

	get appTitle() {
		return this.$$.ctx[0];
	}

	set appTitle(appTitle) {
		this.$$set({ appTitle });
		flush();
	}

	get version() {
		return this.$$.ctx[1];
	}

	set version(version) {
		this.$$set({ version });
		flush();
	}

	get domain() {
		return this.$$.ctx[2];
	}

	set domain(domain) {
		this.$$set({ domain });
		flush();
	}

	get copyright() {
		return this.$$.ctx[3];
	}

	set copyright(copyright) {
		this.$$set({ copyright });
		flush();
	}

	get imageUrl() {
		return this.$$.ctx[4];
	}

	set imageUrl(imageUrl) {
		this.$$set({ imageUrl });
		flush();
	}

	get hide() {
		return this.$$.ctx[5];
	}

	set hide(hide) {
		this.$$set({ hide });
		flush();
	}

	get getWebComponent(): HTMLSeAuthenticationElement | undefined {
		return this.$$.ctx[7];
	}
}

export default SeAuthentication;